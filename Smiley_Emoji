function setup() { 
  createCanvas(400, 400);
} 

function draw() { 

	ellipse(80, 80, 148, 148);
	arc(80, 100, 65, 30, 0, PI)
	fill(0);
	
  textSize(28);
  text('Colt Couture', 5, 180);
  
	ellipse(61, 60, 10, 20); 	
	ellipse(96, 60, 10, 20);  
	fill(255, 255, 0);
}
